import React from 'react';

const Logo = ({className}) => {
    return (
        <div>
            <h1 className={`${className}`}>
                <span>e</span>
                <span>TuitionBD</span>
            </h1>
        </div>
    );
};

export default Logo;